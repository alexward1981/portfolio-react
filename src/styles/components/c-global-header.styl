.c-global-header {
  position: absolute;
  top:0;
  z-index: 100;
  .menu-closed & {
    display: none;
  }
  .menu-open & {
    background-color: rgba($dark, .7);
    height: 100vh;
    width: 70vw;
    overflow:hidden;
    right:0;
    left: auto;
    }

  +above(m) {
    .menu-closed &,
    .menu-open & {
      display: block;
      background-color: transparent;
      height: auto;
      cf();
      left:0;
      right: auto;
      width: 100%;
    }
  }
}
